Algoritmo "tabuada"

Var

   i, N, x : inteiro

Inicio

   escreval("Deseja a tabuada para qual valor? ")
   leia(N)
   
   para i de 1 ate 10 faca
      x <- N * i
      escreval(N, " x ", i, " = ", x:2)
   fimpara

Fimalgoritmo